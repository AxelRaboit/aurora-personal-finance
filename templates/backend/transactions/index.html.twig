{% extends '@Core/backend/layout.html.twig' %}

{% block title %}{{ 'backend.nav.personal_finance_transactions'|trans }} - {{ parent() }}{% endblock %}

{% block page_header_slot %}
    {{ include('@Shared/components/page_header.html.twig', {
        crumbs: [
            {label: 'backend.nav.sections.personal_finance'|trans},
            {label: 'backend.nav.personal_finance_transactions'|trans},
        ],
    }) }}
{% endblock %}

{% block body %}
<div {{ vue_component('personal-finance/backend/transaction/PersonalFinanceTransactionsApp', {
    wallets: wallets,
    categoriesByWallet: categoriesByWallet,
    transactionsByWallet: transactionsByWallet,
    types: types,
    createTransactionPath: createTransactionPath,
    updateTransactionPath: updateTransactionPath,
    deleteTransactionPath: deleteTransactionPath,
}) }} class="flex-1 min-w-0"></div>
{% endblock %}
